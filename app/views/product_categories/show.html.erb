<h1><%= @product_category.name %></h1>

<p><%= @product_category.description %></p>

<ul>
  <li><%= link_to "Edit", edit_product_category_path(@product_category) %></li>
  <li><%= link_to "Destroy", product_category_path(@product_category),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>